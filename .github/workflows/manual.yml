name: Manual
on:
  workflow_dispatch:
    inputs:
      greeting:
        type: string
        default: Hello
        required: true
        descriotion: A cheerful word
jobs:
  run:
    runs-on: Ubuntu-latest
    steps:
      - run echo "${{ inputs.greeting }}"
